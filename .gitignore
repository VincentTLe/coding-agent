__pycache__/
*.pyc
*.pyo
.venv/
.env
.DS_Store
*.log
vllm_logs/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.ipynb_checkpoints/

# Eval run artifacts — generated at runtime, not source.
# (The benchmark TASK dirs under eval/tasks/bench/ ARE committed; only the
#  run outputs, per-task logs, and the downloaded benchmark cache are ignored.)
eval/results/
eval/.cache/

# Personal documents / PII — never commit to this public repo
*.pdf
*.docx
Transcript*.pdf
Resume*.pdf

# Local session/handoff logs and timestamped backups — not source.
.agent/
*.bak-*
