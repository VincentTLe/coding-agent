"""Geometric shape calculations — all stub implementations return 0.

The agent must replace each stub with a real implementation per the docstring
and the test expectations.
"""

import math


def area_circle(radius: float) -> float:
    """Return the area of a circle with the given radius. area = pi * r^2."""
    # TODO: implement.
    return 0.0


def perimeter_rectangle(width: float, height: float) -> float:
    """Return the perimeter of a rectangle. perimeter = 2 * (width + height)."""
    # TODO: implement.
    return 0.0


def volume_cylinder(radius: float, height: float) -> float:
    """Return the volume of a cylinder. volume = pi * r^2 * h."""
    # TODO: implement.
    return 0.0


def hypotenuse(a: float, b: float) -> float:
    """Return the hypotenuse of a right triangle with legs a and b."""
    # TODO: implement (Pythagorean theorem).
    return 0.0
