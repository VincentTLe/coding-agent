# he_009 — rolling_max

## Goal
from typing import List, Tuple


def rolling_max(numbers: List[int]) -> List[int]:
    """ From a given list of integers, generate a list of rolling maximum element found until given moment
    in the sequence.
    >>> rolling_max([1, 2, 3, 2, 3, 4, 2])
    [1, 2, 3, 3, 3, 4, 4]
    """

Implement `rolling_max` in `rolling_max.py` so all tests pass.

## Category
arrays

## Difficulty
medium

## Tests
hidden

## Source/License
HumanEval/9 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
