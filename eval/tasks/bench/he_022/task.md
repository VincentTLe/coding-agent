# he_022 — filter_integers

## Goal
from typing import List, Any


def filter_integers(values: List[Any]) -> List[int]:
    """ Filter given list of any python values only for integers
    >>> filter_integers(['a', 3.14, 5])
    [5]
    >>> filter_integers([1, 2, 3, 'abc', {}, []])
    [1, 2, 3]
    """

Implement `filter_integers` in `filter_integers.py` so all tests pass.

## Category
data_structures

## Difficulty
medium

## Tests
hidden

## Source/License
HumanEval/22 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
