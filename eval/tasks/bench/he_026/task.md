# he_026 — remove_duplicates

## Goal
from typing import List


def remove_duplicates(numbers: List[int]) -> List[int]:
    """ From a list of integers, remove all elements that occur more than once.
    Keep order of elements left the same as in the input.
    >>> remove_duplicates([1, 2, 3, 2, 4])
    [1, 3, 4]
    """

Implement `remove_duplicates` in `remove_duplicates.py` so all tests pass.

## Category
data_structures

## Difficulty
medium

## Tests
hidden

## Source/License
HumanEval/26 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
