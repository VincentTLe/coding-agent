# he_030 — get_positive

## Goal
def get_positive(l: list):
    """Return only positive numbers in the list.
    >>> get_positive([-1, 2, -4, 5, 6])
    [2, 5, 6]
    >>> get_positive([5, 3, -5, 2, -3, 3, 9, 0, 123, 1, -10])
    [5, 3, 2, 3, 9, 123, 1]
    """

Implement `get_positive` in `get_positive.py` so all tests pass.

## Category
arrays

## Difficulty
medium

## Tests
hidden

## Source/License
HumanEval/30 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
