# he_049 — modp

## Goal
def modp(n: int, p: int):
    """Return 2^n modulo p (be aware of numerics).
    >>> modp(3, 5)
    3
    >>> modp(1101, 101)
    2
    >>> modp(0, 101)
    1
    >>> modp(3, 11)
    8
    >>> modp(100, 101)
    1
    """

Implement `modp` in `modp.py` so all tests pass.

## Category
math

## Difficulty
hard

## Tests
hidden

## Source/License
HumanEval/49 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
