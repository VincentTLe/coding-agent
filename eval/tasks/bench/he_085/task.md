# he_085 — add

## Goal
def add(lst):
    """Given a non-empty list of integers lst. add the even elements that are at odd indices..


    Examples:
        add([4, 2, 6, 7]) ==> 2 
    """

Implement `add` in `add.py` so all tests pass.

## Category
math

## Difficulty
medium

## Tests
hidden

## Source/License
HumanEval/85 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
