# he_104 — unique_digits

## Goal
def unique_digits(x):
    """Given a list of positive integers x. return a sorted list of all 
    elements that hasn't any even digit.

    Note: Returned list should be sorted in increasing order.
    
    For example:
    >>> unique_digits([15, 33, 1422, 1])
    [1, 15, 33]
    >>> unique_digits([152, 323, 1422, 10])
    []
    """

Implement `unique_digits` in `unique_digits.py` so all tests pass.

## Category
math

## Difficulty
hard

## Tests
hidden

## Source/License
HumanEval/104 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
