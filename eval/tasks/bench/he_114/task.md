# he_114 — minSubArraySum

## Goal
def minSubArraySum(nums):
    """
    Given an array of integers nums, find the minimum sum of any non-empty sub-array
    of nums.
    Example
    minSubArraySum([2, 3, 4, 1, 2, 4]) == 1
    minSubArraySum([-1, -2, -3]) == -6
    """

Implement `minSubArraySum` in `minSubArraySum.py` so all tests pass.

## Category
arrays

## Difficulty
hard

## Tests
hidden

## Source/License
HumanEval/114 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
