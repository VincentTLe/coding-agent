# he_131 — digits

## Goal
def digits(n):
    """Given a positive integer n, return the product of the odd digits.
    Return 0 if all digits are even.
    For example:
    digits(1)  == 1
    digits(4)  == 0
    digits(235) == 15
    """

Implement `digits` in `digits.py` so all tests pass.

## Category
math

## Difficulty
medium

## Tests
hidden

## Source/License
HumanEval/131 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
