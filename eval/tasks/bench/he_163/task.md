# he_163 — generate_integers

## Goal
def generate_integers(a, b):
    """
    Given two positive integers a and b, return the even digits between a
    and b, in ascending order.

    For example:
    generate_integers(2, 8) => [2, 4, 6, 8]
    generate_integers(8, 2) => [2, 4, 6, 8]
    generate_integers(10, 14) => []
    """

Implement `generate_integers` in `generate_integers.py` so all tests pass.

## Category
math

## Difficulty
medium

## Tests
hidden

## Source/License
HumanEval/163 via EvalPlus (HumanEval+). HumanEval: MIT; EvalPlus augmented tests: Apache-2.0.
